import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('chat options view allows setting chat receiver of given chat')

test.todo('chat options view allows editing allowed actions taken by agents receiving user messages')

test.todo('chat options view allows deleting given chat')