import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('trigger detail shows name, type and template of trigger')

test.todo('trigger detail can edit name of trigger')

test.todo('trigger detail can delete given trigger')

test.todo('trigger detail shows Poll trigger options when Poll trigger is selected')

test.todo('trigger detail shows Webhook trigger options when Webhook trigger is selected')

test.todo('trigger detail shows Chat trigger options when Chat trigger is selected')

test.todo('trigger detail shows Timer trigger options when Timer trigger is selected')

test.todo('trigger detail can create new trigger (given no id)')
