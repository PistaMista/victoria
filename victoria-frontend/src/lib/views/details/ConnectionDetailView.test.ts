import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('connection detail shows details of Ollama connection')

test.todo('connection detail can edit URL of Ollama connection')

test.todo('connection detail can delete given Ollama connection')

test.todo('connection detail can create new Ollama connection (given no id)')