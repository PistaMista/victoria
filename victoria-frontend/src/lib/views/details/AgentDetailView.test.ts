import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('agent detail shows info about given agent')

test.todo('agent detail can edit permitted actions of given agent')

test.todo('agent detail can edit processed triggers of given agent')

test.todo('agent detail can edit system prompt of given agent')

test.todo('agent detail can edit prompt, actions and triggers of given agent')

test.todo('agent detail can delete given agent')

test.todo('agent detail can create a new agent (with no id given)')
