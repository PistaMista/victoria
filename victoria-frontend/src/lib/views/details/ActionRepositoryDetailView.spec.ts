import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('action repository detail shows info about given repository')

test.todo('action repository detail can edit URL of given repository (leaving name unchanged)')

test.todo('action repository detail can delete a given repository')

test.todo('action repository detail can create a new action repository (with no id given)')
