import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('trigger list view contains all trigger names')

test.todo('trigger list view can search for triggers')

test.todo('clicking create button in trigger list view routes to trigger creation view')
