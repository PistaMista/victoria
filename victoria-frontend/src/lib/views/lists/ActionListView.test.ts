import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('actions list view shows all registered action repositories')

test.todo('clicking add button in actions list view routes to action repository add view')

test.todo('actions list view can search for actions')