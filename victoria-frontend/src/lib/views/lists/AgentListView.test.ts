import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('agent list view contains all registered agent names')

test.todo('agent list view create button routes to agent creation view')

test.todo('agent list view can search for agents')