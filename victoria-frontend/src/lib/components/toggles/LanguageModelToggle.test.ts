import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('language model toggle shows name of LLM with given id')

test.todo('clicking model toggle of disabled model sends enable request')

test.todo('clicking model toggle of enabled model sends disable request')