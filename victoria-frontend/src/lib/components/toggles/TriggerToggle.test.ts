import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('trigger toggle shows name of trigger with given id')

test.todo('clicking trigger toggle of disabled trigger sends enable request')

test.todo('clicking trigger toggle of enabled trigger sends disable request')