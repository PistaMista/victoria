import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('parameter slider textbox shows current value')

test.todo('parameter slider resets to previous value when empty textbox is defocused')

test.todo('parameter slider resets to default value when reset button is clicked')

test.todo('dragging parameter slider all the way left sets minimum value')

test.todo('dragging parameter slider all the way right sets maximum value')
