import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('poll trigger settings allows setting polled URL')

test.todo('poll trigger settings allows setting HTTP method')

test.todo('poll trigger allows setting poll interval')