import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

// This is a similar interface to the one used in the parental controls for the TPLink Archer VR200 router
test.todo('timer trigger allows setting day hours when it executes')