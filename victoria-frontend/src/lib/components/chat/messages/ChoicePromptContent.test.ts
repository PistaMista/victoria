import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('choice prompt shows all available options')

test.todo('clicking on a choice in choice prompt sends that choice as a response to the invocation query')

test.todo('choice prompt hides the choices after successfully sending the choice')