import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('action confirmation message shows details of invocation with given id')

test.todo('pressing "execute" in action confirmation message sends "true" as a response to the invocation query')

test.todo('pressing "abort" in action confirmation message sends "false" as a response to the invocation query')