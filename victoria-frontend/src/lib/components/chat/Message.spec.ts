import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('message shows name of sender')

test.todo('message shows relative time of sending if sent no earlier than yesterday')

test.todo('message shows full date of sending if sent earlier than yesterday')

test.todo('message shows text content')