import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('chat prompt send button does nothing when clicked with an empty prompt')

test.todo('chat prompt can send text message to the chat receiver of the conversation with given id')

test.todo('chat prompt attachment button attaches attachment to sent message')

test.todo('chat prompt options button routes to options of conversation with given id')