import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('monologue status indicator displays status of monologue with given id')

test.todo('monologue status indicator can optionally display runtime of monologue with given id')

test.todo('monologue status indicator can optionally display launch date of monologue with given id')

test.todo('monologue status indicator can optionally display finish date of monologue with given id')