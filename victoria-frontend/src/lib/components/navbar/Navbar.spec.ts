import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('can navigate to /agents via desktop navbar')

test.todo('can navigate to /agents via mobile navbar')

test.todo('clicking navbar expand button opens drawer with navigation buttons')
