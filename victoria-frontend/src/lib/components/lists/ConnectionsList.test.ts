import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('connections list shows all LLM service connection names')

test.todo('pressing add button in connections list routes to the connection create view')