import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('trigger list item shows name of given trigger')

test.todo('trigger list item shows type of given trigger')

test.todo('clicking trigger list item routes to detail of given trigger')