import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('user list item shows name of given user')

test.todo('user list item shows role of given user')

test.todo('pressing configure button in user list item routes to detail of given user')