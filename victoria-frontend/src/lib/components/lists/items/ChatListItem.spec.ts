import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('chat list item shows title of chat with given id')

test.todo('chat list item shows summary of chat with given id')

test.todo('clicking chat list item routes to the chat view for the given chat')