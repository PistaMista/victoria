import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('connection list item shows name of LLM service connection with given id')

test.todo('pressing configure button of connection list item routes to detail of given LLM service connection')