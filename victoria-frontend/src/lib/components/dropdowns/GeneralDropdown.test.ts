import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('general dropdown shows set options when clicked')

test.todo('general dropdown sets option variable when option clicked')

test.todo('general dropdown shows clicked option as selected')