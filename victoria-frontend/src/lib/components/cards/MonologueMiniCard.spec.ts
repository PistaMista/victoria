import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('monologue mini card shows name of monologue with given id', async () => {
})

test.todo('monologue mini card shows status of monologue with given id', async () => {
})

test.todo('monologue mini card routes to monologue detail when clicked', async () => {
})
