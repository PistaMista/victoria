import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('agent mini card shows name of agent with given id', async () => {
})

test.todo('agent mini card shows status icon of agent with given id', async () => {
})