import { expect, test } from "vitest";
import userEvent from '@testing-library/user-event';
import { render, screen } from '@testing-library/svelte';

test.todo('unread message mini card shows title of the chat corresponding to the message with given id', async () => {
})

test.todo('unread message mini card routes on click to the chat corresponding to the message with given id', async () => {
})